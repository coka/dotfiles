# dotfiles

Welcome, traveler! ✨

```sh
curl -fsSL https://install.determinate.systems/nix | sh -s -- install
nix shell nixpkgs#home-manager
home-manager switch --flake .
```

Once `home-manager` is on your path, simply:

```sh
make
```
